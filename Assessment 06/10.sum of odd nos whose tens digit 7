#include <stdio.h>
int main()
{
   int n=10,sum=0;
   loop:if(n<100)
   {
       if(n%2==1)
       {
           int n1=n/10;
           if(n1==7)
           {
               sum=sum+n;
           }
       }
       n++;
       goto loop;
   }
   printf("%d",sum);
}
