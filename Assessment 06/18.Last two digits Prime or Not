#include <stdio.h>
int main()
{
    int n,count=0,n1,n2,i=1;
    scanf("%d",&n);
    n2=n%10;
    n=n/10;
    n1=n%10;
    int digit=n1*10+n2;
    loop:while(i<=digit)
    {
        if(digit%i==0){
        count++;}
        i++;
        goto loop;
    }
    if(count==2)
    printf("Prime");
    else
    printf("Not Prime");
   
}
