#include <stdio.h>
int main()
{
    int n,count=0;
    scanf("%d",&n);
    while(n>0)
    {
        int n1=n%10;
        int i=n1;
        while(i>=1)
        {
            if(i*i==n1)
            count++;
            i--;
        }
        n=n/10;
        
    }
    printf("%d",count);
}
