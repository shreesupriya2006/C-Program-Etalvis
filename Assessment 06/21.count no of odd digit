#include <stdio.h>
int main()
{
    int n,count=0;
    scanf("%d",&n);
    loop:if(n>0)
    {
        int n1=n%10;
        if(n1%2==1){
        count++;}
        n=n/10;
        goto loop;
    }
    printf("%d",count);
}
