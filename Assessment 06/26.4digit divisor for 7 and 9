#include <stdio.h>
int main()
{
    int n=1000,divi;
    loop:while(n<10000)
    {
        if((n%7==0)&&(n%9==0))
        {
            divi=n;
        }
        n++;
        goto loop;
    }
    printf("%d",divi);
}
