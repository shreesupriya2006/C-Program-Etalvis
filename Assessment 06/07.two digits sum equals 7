#include <stdio.h>
int main()
{
   int n=10,sum=0;
   loop:if(n<100)
   {
       if(n%2==1)
       {
           int n1=n/10;
           int n2=n%10;
           sum=n1+n2;
           if(sum==7)
           {
               printf("%d\n",n);
           }
       }
       n++;
       goto loop;
   }
}
