#include <stdio.h>
int main()
{
    int n=10,sum=0,n1;
    loop: if(n<100)
    {
        n1=n%10;
        if(n1==5)
        {
            sum=sum+n;
        }
        n++;
        goto loop;
    }
    printf("%d",sum);
}
