#include <stdio.h>
int main()
{
    int i=0,valid=1,len=0;
    char a[51];
    scanf("%s",a);
    while(a[i]!='\0')
    {
        if(a[i]<'0'||a[i]>'9')
        {
            valid=0;
            break;
        }
        len++;
        i++;
        
    }
    if(len==0||len>50)
    valid=0;
    if(valid)
    printf("Valid number");
    else
    printf("Invalid number");
    
}
