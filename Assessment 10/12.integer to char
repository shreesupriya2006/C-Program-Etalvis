#include <stdio.h>
#include <string.h>
int main()
{
   int a[51],i=0,n;
   char s[51];
   scanf("%d",&n);
   for(i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
       s[i]=a[i]+'0';
   }
   s[n]='\0';
   printf("%s",s);
}
