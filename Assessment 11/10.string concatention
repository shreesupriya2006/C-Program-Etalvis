#include <stdio.h>
void strcon(char *p1 ,char *p2,char *p3);
int main()
{
    int s1,s2,i;
    scanf("%d %d",&s1,&s2);
    char a[s1],b[s2],c[s1+s2];
    scanf("%s",a);
    scanf("%s",b);
    char *p1,*p2,*p3;
    p1=&a[0];
    p2=&b[0];
    p3=&c[0];
    strcon(a,b,c);
    printf("%s ",c);
}
void strcon(char *p1 ,char *p2,char *p3)
{
    int i;
        while(*p1!='\0')
        {
            *p3=*p1;
            p3++;
            p1++;
        }
        while(*p2!='\0')
        {
            *p3=*p2;
            p3++;
            p2++;
        }
        *p3='\0';
}
