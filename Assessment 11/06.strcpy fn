#include <stdio.h>
#include <string.h>
void strcopy(char *src,char *dst);
int main()
{
    int size;
    scanf("%d",&size);
    char src[size],dst[size];
    scanf("%s ",src);
    strcopy(src,dst);
    printf("%s",dst);
}
void strcopy(char *src,char *dst)
{
   strcpy(dst,src);
}
