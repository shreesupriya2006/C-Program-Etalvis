#include <stdio.h>
void disp_reverse_number(int);
int main()
{
    int n;
    scanf("%d",&n);
    disp_reverse_number(n);
}
void disp_reverse_number(int n)
{
    int rev=0;
    while(n>0)
    {
        rev=rev*10+n%10;
        n=n/10;
    }
    printf("%d",rev);
}
