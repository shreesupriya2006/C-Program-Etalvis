#include <stdio.h>
void count_total_digit(int);
int main()
{
    int a;
    scanf("%d",&a);
    count_total_digit(a);
}
void count_total_digit(int a)
{
    int count=0;
while(a>0)
{
count++;
a=a/10;}
    printf("%d",count);
}
