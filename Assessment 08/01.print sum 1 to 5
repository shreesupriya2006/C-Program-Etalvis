#include <stdio.h>
int disp_sum(int);
int main()
{
    int sum;
    int n=1;
    sum=disp_sum(n);
    printf("%d",sum);
}
int disp_sum(int a)
{
    int sum=0;
    loop:if(a<6)
    {
        sum=sum+a;
        a++;
        goto loop;
    }
    return sum;
}
