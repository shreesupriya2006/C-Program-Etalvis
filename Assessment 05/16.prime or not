#include <stdio.h>
int main()
{
    int n,prime=1;
    scanf("%d",&n);
    int i=2;
    while(i<n)
    {
        if(n%i==0)
        {
            prime=0;
            break;
        }
        i++;
    }
    if(prime)
    printf("Prime");
    else
    printf("Not Prime");
}
