#include <stdio.h>
int main()
{
    int n,count=0;
    scanf("%d",&n);
    while(n>0)
    {
        int n1=n%10;
        if(n1%2==1)
        count++;
        n=n/10;
    }
    printf("%d",count);
    
}
