#include <stdio.h>
int main()
{
    int n=1,i,count=0,count1;
    loop:if(n<10)
    {
        i=1,count1=0;
        while(i<=n){
        if(n%i==0)
         count1++;
        i++;}
        n++;
        if(count1==2)
        count++;
        goto loop;
    }
    printf("%d",count);
    
    
}
