#include<stdio.h>
int main ()
{
    char str[100];
    char *p;
    int count=1;
    gets(str);
    p=str;
    while(*p!='\0')
    {
        if(*p==' '&&*(p+1)!=' ')
            count++;
        p++;
    }
    printf("%d",count);
}
