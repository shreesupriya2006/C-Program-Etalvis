#include <stdio.h>
int main()
{
    int n,i=0,cnt=0;
    int a[100];
    while(1)
    {
        scanf("%d",&n);
        if(n==0)
            break;
        a[i]=n;
        i++;
    }
    if(i%2==1)
        printf("%d",a[i/2]);
    else
        printf("%d",(a[i/2]+a[(i/2)-1])/2);
}
