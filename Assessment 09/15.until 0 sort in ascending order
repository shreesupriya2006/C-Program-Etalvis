#include <stdio.h>
int main()
{
    int i,n,k=0;
    int a[100];
    while(1)
    {
        scanf("%d",&n);
        if(n==0)
            break;
        a[k]=n;
        k++;
    }
    for(int i=0;i<k;i++)
    {
        int j=i;
        while(j<k-1){
        if(a[i]>a[j+1])
        {
            int temp=a[i];
            a[i]=a[j+1];
            a[j+1]=temp;
        }
        j++;}
    }
    for(int j=0;j<k;j++)
        printf("%d ",a[j]);
}
