#include <stdio.h>
int main()
{
    int n,i,cnt=0;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
        if(a[i]==0)
        {
            goto end;
        }
        cnt++;
        if(cnt==4)
            goto end;
    }
    end:
    if(cnt==4)
    {
     if(a[0]==a[3])
        printf("Sucess");
    else
        printf("Failure");}
}
