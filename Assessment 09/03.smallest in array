#include <stdio.h>
int main()
{
    int a[5],i,small;
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
    }
    small=a[0];
    for(i=1;i<5;i++)
    {
        if(small>a[i])
        small=a[i];
    }
    printf("%d",small);
}
